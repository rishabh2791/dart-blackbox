import 'package:blackbox/core/renderer/node.dart';
import 'package:blackbox/core/renderer/renderer.dart';
import 'package:blackbox/core/renderer/statement.dart';

class XMLRenderer implements Renderer{
  //TODO complete implementation
  @override
  List<Node> findNodes(){
    List<Node> node = <Node>[];
    return node;
  }

  @override
  List<Statement> findStatements(){
    List<Statement> statement = <Statement>[];
    return statement;
  }

  @override
  String renderNodes(){
    String rendered = "";
    return rendered;
  }

  @override
  String renderStatements(){
    String rendered = "";
    return rendered;
  }

  @override
  String render(){
    String rendered = "";
    return rendered;
  }
}